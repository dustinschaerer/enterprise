- flash.each do |name, msg|
  - if msg.is_a?(String)
    #flash_messages.container-fluid
      .row
        .col-xs-12
          .alert.bg-for-city
            = content_tag :div, msg, :id => "flash_#{name}"